apiVersion: v1
kind: Service
metadata:
  name: {{ include "web-app-minio.fullname" . }}
  labels:
    {{- include "web-app-minio.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "web-app-minio.selectorLabels" . | nindent 4 }}
