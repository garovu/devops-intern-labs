apiVersion: v1
kind: Service
metadata:
  name: {{ include "lgtm-stack.fullname" . }}
  labels:
    {{- include "lgtm-stack.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "lgtm-stack.selectorLabels" . | nindent 4 }}
