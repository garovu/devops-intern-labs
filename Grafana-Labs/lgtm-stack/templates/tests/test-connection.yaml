apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "lgtm-stack.fullname" . }}-test-connection"
  labels:
    {{- include "lgtm-stack.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "lgtm-stack.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
